// Curtains
[oploc1,inaccastledoubledoorlopen]
// Temp note: dur does not need updated
loc_change(inaccastledoubledoorr, 200);

[oploc1,inaccastledoubledoorr]
// Temp note: dur does not need updated
loc_change(inaccastledoubledoorlopen, 200);
